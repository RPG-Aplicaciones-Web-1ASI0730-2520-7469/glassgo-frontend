{
  // ============================================================
  // 📦 GlassGo Frontend — package.explained.jsonc
  // ------------------------------------------------------------
  // Core configuration file for the GlassGo Frontend project.
  // Defines metadata, dependencies, and npm scripts for:
  //  - Local development (Vite + JSON Server)
  //  - Building and previewing production assets
  //  - Maintaining modular DDD architecture
  // ============================================================

  "name": "glassgo-frontend", // Project name (must match GitHub repo)

  "private": true,            // Prevents accidental npm publication

  "version": "0.0.1",         // Initial semantic version: MAJOR.MINOR.PATCH

  "type": "module",           // Enables ES Modules syntax (import/export)

  // ------------------------------------------------------------
  // 🧩 Scripts (npm run ...)
  // ------------------------------------------------------------
  "scripts": {
    "dev": "vite",                             // Starts local dev server → http://localhost:5173
    "build": "vite build",                     // Bundles optimized production files → /dist
    "preview": "vite preview",                 // Serves the built /dist folder locally
    "server": "json-server --watch server/db.json --routes server/routes.json --port 3000"
    // Launches mock API with JSON Server → http://localhost:3000
    // - Watches db.json (mock database)
    // - Applies route rules from routes.json
    // ----------------------------------------------------------
  },

  // ------------------------------------------------------------
  // 📚 Dependencies — Core runtime packages
  // ------------------------------------------------------------
  "dependencies": {
    "axios": "^1.7.7",         // HTTP client for API calls (GET/POST/PUT/DELETE)
    "pinia": "^2.1.7",         // Vue’s official global state manager
    "vue": "^3.4.21",          // Vue 3 framework (Composition API)
    "vue-i18n": "^9.14.0",     // i18n plugin for multilingual support
    "vue-router": "^4.3.0"     // Official router for page navigation
  },

  // ------------------------------------------------------------
  // 🛠️ Dev Dependencies — Used only in local builds
  // ------------------------------------------------------------
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",   // Vite plugin to compile .vue components
    "json-server": "^0.17.4",         // Local mock REST API for testing
    "vite": "^5.0.8"                  // Lightning-fast dev server and bundler
  },

  // ------------------------------------------------------------
  // 🧠 Recommended Node.js Engine
  // ------------------------------------------------------------
  "engines": {
    "node": ">=18.0.0"
  },

  // ------------------------------------------------------------
  // 🧾 Metadata (for documentation / CI)
  // ------------------------------------------------------------
  "author": "RPG-Aplicaciones-Web-1ASI0730-2520-7469",         // Project author or organization
  "license": "MIT",                       // Open-source license
  "description": "Official frontend repository for the GlassGo Project by RPG Startup. Built with Vue 3, Vite, and PrimeVue. Implements the web application layer of the system architecture, integrating with the internal RESTful API and supporting i18n, responsive UI, and modular development under GitFlow and Conventional Commits."
}
